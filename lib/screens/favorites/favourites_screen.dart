import 'package:flutter/material.dart';

class FavouiteScreen extends StatelessWidget {
  const FavouiteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text("This is favourite screen"),
    );
  }
}